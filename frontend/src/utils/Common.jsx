import React from "react";

const Common = () => {
    return (
        <div>
            {/* --- COLOR PALETTE --- */}
            {/* Primary: #1a202c (Dark blue/gray) */}
            {/* Secondary: #4a5568 (Gray) */}
            {/* Accent: #3182ce (Blue) */}
            {/* Light background: #f7fafc */}
            {/* Border: #e2e8f0 */}

            {/* --- FONT FAMILY --- */}
            {/* Base font: 'Helvetica Neue', Helvetica, Arial, sans-serif */}
            {/* Headings font-weight: 600 or 700 */}
            {/* Body font-weight: 400 */}

            {/* --- FONT SIZES --- */}
            {/* h1: 2.25rem (36px) */}
            {/* h2: 1.875rem (30px) */}
            {/* h3: 1.5rem (24px) */}
            {/* Paragraph: 1rem (16px) */}

            {/* --- HOVER STATES --- */}
            {/* Button hover: background #e2e8f0, text #1a202c */}
            {/* Link hover: underline + color #3182ce */}

            {/* --- RADIUS & SHADOW --- */}
            {/* Border radius: 0.5rem (rounded-lg or rounded-full for pills) */}
            {/* Shadow: small = sm, medium = md for cards/buttons */}

            <h2>Common design tokens are documented in comments above.</h2>
        </div>
    );
};

export default Common;
